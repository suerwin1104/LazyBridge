# Dev Cycle Workflow

---

description: 自動化開發、測試與驗證循環 (Dev Cycle)
---

// turbo-all

1. 確保開發環境正常
2. 執行代碼檢查 (Linting): `npm run lint` 或 `flake8 .`
3. 執行單元測試: `npm test` 或 `pytest`
4. 驗證新功能的邏輯
5. 如果一切正常，回報任務完成
